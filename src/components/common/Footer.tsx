import Link from "next/link";

const Footer: React.FC = () => {
  return (
    // animate border bottom on load from left to right
    <footer className="border-anim fixed top-0 z-50 w-full bg-gradient-to-b from-primary/40 to-transparent backdrop-blur-sm"></footer>
  );
};
export default Footer;
